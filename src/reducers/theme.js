const INITIAL_STATE = {
  fontColor: '#262938',
  mainColor: '#fe4d10',
};

export default function themeReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
