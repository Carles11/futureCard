const INITIAL_STATE = {
  fontColor: '#ffffff',
  mainColor: '#fe4d10',
  background: '#262938',
};

export default function themeReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
